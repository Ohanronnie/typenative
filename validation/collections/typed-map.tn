import { Map } from "std/collections";
function main(): i32 {
  let map = new Map<i32, i32>({
    capacity: 8,
  });
  let key: i32 = 16;
  map.set(key, 42);
  const output = map.get( &key);
  if (output === undefined) {
    return 1;
  }
  unsafe {
    return * output!;
  }
}
