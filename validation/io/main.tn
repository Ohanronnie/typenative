import { fromStatic } from "std/bytes";
import { IOError, stdout } from "std/io";

function main(): void throws IOError {
  let out = stdout();
  let bytes = fromStatic("hello\\n");
  try out.writeAll( &bytes);
}
