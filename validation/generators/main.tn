import { Iterable } from "std/core";
function* numbers(): Iterable<i32> {
  yield 16;
  yield 26;
}
function main(): i32 {
  let result: i32 = 0;
  for (const value of numbers()) {
    result = result + value;
  }
  return result;
}
