
@Clone struct User {
  public id: u64;
  public name: string;
}
function main(): i32 {
  const original = new User({
    id: 42u64, name: string.from("clone"),
  });
  const copied = original.clone();
  if (copied.id !== 42u64 || copied.name !== "clone") {
    return 1i32;
  }
  return 42i32;
}
